[project]
name = "unihousing-backend"
version = "0.1.0"
description = "Simple fastAPI server using uv"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.121.2",
    "pymongo>=4.15.4",
    "python-dotenv>=1.2.1",
    "uvicorn>=0.38.0",
]
